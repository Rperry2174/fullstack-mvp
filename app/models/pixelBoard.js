var mongoose = require('mongoose');

module.exports = mongoose.model('PixelBoard', {
    // text: {
    //     type: String,
    //     default: '0, 0, 0, 0'
    // }
    //eventually needs to be an array
});